(function(angular){
  'use strict';
  /*
  const env = angular.module('waiter');
  env.constant("path",{
    Get:function () {
      return $location.path();
    }
  });
  //AppRoute.constant("Urlapi","https://directoriochocoonline.com/api/");
  env.constant("Urlapi", "http://localhost:8000/api/");
  env.constant("host", "https://directoriochocoonline.com/");
  env.constant("Msg",{
    Box:function(title, cont, type) {
      if (type == 1){

      }else if (type == 2) {

      }else if (type == 3) {

      }
    },
    Toast:function(title, cont){
      Materialize.toast(title + ": " + cont, 3000);
    },
    Log:function(title, cont) {
      console.log(title +": "+ cont);
    },
    Error:function(title, cont) {
      console.error(title +": "+ cont);
    },
    Alert:function(title, cont){
      alert(title + cont);
    }
  });
  env.constant("isMobile",{
    Android:function() {
      return navigator.userAgent.match(/Android/i);},
    BlackBerry:function(){
      return navigator.userAgent.match(/BlackBerry/i);},
    iOS:function() {
      return navigator.userAgent.match(/iPhone|iPad|iPod/i);},
    Opera:function(){
      return navigator.userAgent.match(/Opera Mini/i);},
    Windows:function(){
      return navigator.userAgent.match(/IEMobile/i);},
    isMovil:function(){
      return (this.Android() || this.BlackBerry() || this.iOS() || this.Opera() || this.Windows());
    }
  });
  */
})(window.angular);
